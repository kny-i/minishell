#include "minishell.h"
#include <unistd.h>



int cd_core(t_cmd *cmd, t_envp *env)
{/*
	char	**args;
	char	**env_array;
	int 	status;

	args = list_to_args(cmd);
	env_array = list_to_env(env);
	if (cmd->args == NULL)
	{
		cd_home(env);
	}*/

	return (0);
}